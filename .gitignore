# Internal only. BRIEF.md contains pitch strategy and notes about the owner
# that must never appear in a public repo.
BRIEF.md
_unused/

# working backups, never ship
preform-backup/

# source photos stay local. only the WebP versions are served.
images/*.jpg

# local dev only, contains machine-specific absolute paths
.claude/

Thumbs.db
desktop.ini
.DS_Store

